# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'firestore-sample-app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for firestore-sample-app
  pod 'Firebase/Firestore'
  pod 'FirebaseFirestoreSwift'

  target 'firestore-sample-appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'firestore-sample-appUITests' do
    # Pods for testing
  end

end
